# Backlog

Human-maintained follow-ups for this repo. **Not** session goals (those stay in chat per `.agent/phases/developing.md`). Completed work moves to **`LOGBACK.md`** via `/daf-backlog-work`.

## Open

- [ ] multi agent work (ignore for now)
- [ ] **`/shared-product`** (`daf-shared-product`) — multi-repo product docs: global folder under `~/.config/agent/products/<productId>/`, pointer in each linked repo’s `.agent/config.json`; shared PRD/glossary/architecture readable/updatable from every consumer repo (app, website, admin, etc.)

## In progress

- (empty — at most one item while `/daf-backlog-work` is active)
