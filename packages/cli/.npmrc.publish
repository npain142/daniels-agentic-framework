// Use with: NPM_TOKEN=npm_... npm publish --userconfig .npmrc.publish --access public
// Token must be granular: Publish on @daniels-agent-framework/* + "Bypass 2FA"
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
