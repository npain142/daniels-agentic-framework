# Learnings

Short bullets promoted from `/retro` after non-trivial work.

- Backlog: `BACKLOG.md` + `/backlog-add` / `/backlog-work`; done items archive to `LOGBACK.md` (7d/14d prune, `#keep` exempt). Refresh globals with `daf global-setup --platform cursor` after manifest changes.
