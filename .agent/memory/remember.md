# Remember

Standing instructions for the agent on **this project**. Add via `/daf-remember`.

- The `daf` CLI stays decoupled from agent runtime: templating and filesystem copy only — no LLM, no running agents.
- Do not call out or summarize updates to documentation or rules in replies unless the user explicitly asked for those edits or asked what changed.
