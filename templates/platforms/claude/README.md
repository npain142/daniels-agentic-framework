# Claude platform (stub)

Future: installer for Anthropic Claude Code–specific paths analogous to `templates/platforms/cursor/`.

v1 ships **Cursor** only; generic `daf global-setup` covers `~/.config/agent`.
