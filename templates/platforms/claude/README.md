# Claude Code platform

- **Global:** `installClaudeGlobalSkills` → `~/.claude/skills/daf-*/SKILL.md`
- **Project template:** `project/.claude/rules/daf.md` merged by `/setup` when `claude` is in `platforms.json`
