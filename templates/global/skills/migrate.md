# Skill: /daf-migrate

**Deprecated — use `/setup`.** Brownfield adoption is **`/setup`**: inventory → interview (same minimum as `/grill-me`) → merge scaffold (missing paths only, preserve `config.json`) → populate PRD / glossary / architecture from the session.

If you landed here from habit: run **`/setup`**, then **`/phase-transition`** when planning exit criteria pass.
