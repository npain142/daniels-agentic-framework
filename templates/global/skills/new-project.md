# Skill: /new-project

**Deprecated — use `/setup`.** Project setup is there: **`daf global-setup`** on the machine first, then **`/setup`** in the IDE (greenfield = scaffold only; brownfield = interview then structure + docs).

If you landed here from habit: run **`/setup`**. Use **`/grill-me`** only when you want an extra PRD pass after structure exists — not required to finish greenfield `/setup`.
